const router = require("express").Router();

router.get('/')

router.get('/:groupId')

router.post('/')

router.post('/:groupId')

router.put('/:postId')

router.delete('/:postId')

module.exports = router;
